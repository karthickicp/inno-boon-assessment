import { Box } from "@mui/material";
import React from "react";

const Header = () => {
  return (
    <Box
      sx={{
        background: "red",
        color: "white",
        fontSize: "2rem",
        padding: "1rem",
      }}
    >
      Header
    </Box>
  );
};

export default Header;
