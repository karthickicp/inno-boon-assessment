import { Box } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Box
      sx={{
        background: "red",
        color: "white",
        fontSize: "2rem",
        padding: "1rem",
      }}
    >
      Footer
    </Box>
  );
};

export default Footer;
